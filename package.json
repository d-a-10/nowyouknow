{
  "name": "nowyouknow-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd cdn && npm run build",
    "start": "npm run start --prefix cdn",
    "develop": "npm run develop --prefix cdn",
    "postinstall": "cd cdn && npm install && npm rebuild"
  },
  "engines": {
    "node": ">=18.0.0 <=22.x.x",
    "npm": ">=6.0.0"
  }
}
